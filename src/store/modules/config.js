import FormInputs from '../../configs/FormInputs';
import FormOptions from '../../configs/FormOptions';

const state = {
    Forms: {
        FormInputs,
        FormOptions
    }
}

export default {
    namespaced: true,
    state
}